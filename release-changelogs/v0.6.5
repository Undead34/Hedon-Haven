- Skip buttons replaced with double-tap gesture
- Fixed loading spinner not dismissing after video controller initialization
- fvp: vod optimization enabled; audio cracking fixed
- Added `PrivateAuthorProfileException` and detection to xhamster
- Fixed xhamster progress thumbnail scraping
- Added support for special author page URLs on xhamster
- Fixed race condition causing weird duration values
- Fixed database storing duration as string instead of int
- Fixed missing thumbnail data on history screen
- Fixed index exception on empty comments list
- Check if all homepage plugins are disabled on `reloadVideoList`
- Force-download provider icons if missing (instead of only every 5th launch)
- Minimum iOS version set to 16
- Use Java 21 to fix Android release build errors

**Full Changelog**: https://github.com/Hedon-Haven/Hedon-Haven/compare/v0.6.4...v0.6.5
